spring:
  # profile
  profiles:
    active: dev
  # nacos
  cloud:
    nacos:
      discovery:
        server-addr: http://****
        namespace: ****
        group: user-service
      config:
        server-addr: ****
        namespace: *****
        group: user-service
        file-extension: yaml
        prefix: application

